---
title: "{{title}}"
summary: "{{summary}}"
---

# {{title}}
（标题须为中文：类型 + 位置/入口；产品名、类名、CVE 编号可保留原文。不要只用英文标题。）

## 漏洞描述
（产品功能定位一句话简介 + 漏洞成因概要。与提交页「漏洞描述」保持一致。）

一句话写清产品是什么、做什么。

再写漏洞成因概要（缺了什么校验 / 输入如何到达 sink），并说明与历史漏洞文档的关系（原 CVE/补丁与当前变体或仍可利用点）。利用方式与后果放到「漏洞危害」与「漏洞技术细节」，不要在本节展开。

## 漏洞危害
（与提交页「漏洞危害」保持一致。客观描述已证明危害，并准确描述潜在危害。）

- 已证明危害：
- 潜在危害：
- SQL 注入须明确：是否能获取 OS-Shell：是 / 否 / 未验证
- SSRF 须明确：观察面：有回显 / 外带内网信息 / 仅响应差别（内网端口探测） / 不适用

## 漏洞厂商全称
-

## 已知受影响产品及版本
（列出具体产品与版本。无则填「暂未明确」，并给出互联网资产与产品的强相关证明，如「关于」「CopyRight」等可验证截图或摘录。）

-

## 互联网资产证明
> 用于在公开资产测绘平台定位同类应用资产；优先使用应用自身稳定特征，不把漏洞路径、PoC 参数或一次性业务数据当作唯一指纹。测绘语句不允许出现「或」关系。

### 精准测绘语法

#### FOFA
```text

```

#### X 情报社区
```text

```

## 漏洞技术细节

### 补丁绕过简析
（历史漏洞绕过路径必填，置于本节首位。简述原漏洞与官方补丁/修复方式；说明当前源码中补丁是否完整、绕过或仍可利用的原因（如只修代表点、黑名单可绕、同类接口未修、owner 为空绕过权限等）。未修复（unpatched）则写「原洞在当前版本仍未修复」及与文档描述的对照。）

-

### Source → Sink
-

### 漏洞代码
（局部验证 Confirm 时必填：给出漏洞相关源码原文，并写明仓库内完整相对路径，不要只写类名/方法名。静态或靶场动态确认可不填。）

- 完整路径：`src/.../File.ext`（可附行号，例如 `src/.../File.ext:42`）

```text

```

### 完整 PoC 描述
（文字 + 请求包，请勿只放截图。有 HTTP 面时：可运行脚本见同目录 `poc.py`，`python poc.py -u <目标>`；须支持 `--proxy`（空则直连）；RCE 另支持 `-c/--cmd` 并打印回显；输出默认英语，`--zh` 切中文。纯库洞：仅当安装真实包后能调用公开 API 时才写 `poc.py`（不要假 `-u`、不要复制 `harness.py`）；无 HTTP/安装面时写 API 调用配方即可。）

```http

```

### 触发条件
（例如：无需登录 / 需普通用户权限 / 需特定功能模块访问权限。并写明是默认配置还是须改应用自身配置；官方已警示的风险开关不算特定配置。）

-

## 同根因受影响点
（同一根因、同一危害下的其余方法/接口。代表点写在上方「位置」；此处列出全部受影响点，不要为每个方法另交一份报告。）

- `path:line` methodName — 与代表点的差异（若无差异可省略）

## 复现证明
（以典型场景为例。本平台以白盒为主。）

### 基础环境搭建
（不要在本报告复述镜像、端口、凭据或启动命令。统一引用 Reviewer 落盘的动态环境文档。）

详见项目文档 `docs/lab.md`。

若该文档尚不存在，写「动态环境尚未落盘，见 `docs/lab.md`」。

### 漏洞触发操作
（按实际验证方式填写，删去不适用的部分。）

#### 局部验证（harness）

harness 脚本（`harness.py`）在沙箱中执行结果如下（粘贴 stdout 关键输出，截取关键行）：

```text

```

**为何 harness 能证明漏洞存在**：分析 harness 如何构造输入、调用了哪些源码函数、最终观察到了什么运行时行为（如异常/返回值的危险变化、状态篡改、敏感数据外泄等），说明该行为在源码层面与漏洞根因的对应关系。不要写 PoC 的使用方法。

#### 动态验证（靶场 PoC）

关键利用 HTTP 请求（`Host` 用 `TARGET`；body 中长字符串用占位符）：

```http

```

PoC 使用方法（`python poc.py -u <目标>`；RCE 另 `-c/--cmd`；代理用 `--proxy`）：

```text
python poc.py -u http://TARGET:PORT
python poc.py -u http://TARGET:PORT --zh
python poc.py -u http://TARGET:PORT --proxy http://127.0.0.1:8080
python poc.py -u http://TARGET:PORT -c "id"
```

**为何 PoC 能利用该漏洞**：分析请求中哪一字段/参数为攻击者可控输入，经过源码哪条链路最终到达 sink，造成何种危害。说明 PoC 输出的实际结果（响应正文、日志、文件或权限变化）与危害的对应关系。

### 预期证据
（说明成功利用后应观察到的响应、日志、文件或权限变化。SSRF 须与观察面一致：有回显则给出目标正文摘录；外带内网信息则给出从攻击者信道取回的内容摘录；仅差别则给出通/不通对照。）

-

### 复现注意事项
-

## 修复方案
（与提交页「修复方案」保持一致。给出临时措施；厂商已有补丁时可附信息或链接。）

-

## 备注
（无补充则写「无」。可注明对应历史漏洞文档路径或 CVE。）

-
