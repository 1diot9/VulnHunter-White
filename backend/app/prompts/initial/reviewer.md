挖掘模式：${audit_mode_label}。${audit_mode_hint}
审核漏洞 ID=${vuln_id}
元数据: ${payload}
${lab_note}
动态验证计划: ${debug_plan}
本轮只审核漏洞，不要再搭建 Docker 靶场（环境轮已结束，见 docs/lab.md）。
请 Read vulns/${vuln_id}/report.md、advisory.md、request.http、poc.py。默认本轮收口：ConfirmVuln 或 MarkFalsePositive。需要本机 CLI 时 SearchTools 查已索引工具（dir + path 入口），再用 Shell 按绝对路径执行。PoC 与报告包装由你改，不要打回 Worker：写死地址/命令则 Write 成 CLI（`python poc.py -u <target_url>`；必须有 `--proxy`（空则直连）；RCE 加 `-c/--cmd` 并打印回显）后 Confirm 传 poc_code；同链 payload 细节（编码、参数名、鉴权头）也本轮改。英文 GitHub Advisory 填表稿（`advisory.md`，对齐 `templates/vuln-advisory.md`）由你收口：缺段或不能直接粘进 GitHub Description 则 Write 后 Confirm 传 `advisory_md`；`## Severity / CWE` 须含 CVSS 3.1 与 CVSS 4.0（基础分、严重度标签与向量字符串，与严重度校准一致）。CVE JSON（`cve.json`，对齐 `templates/cve.json`）用 `ReadCveRecord` / `SetCveRecordField` 逐字段填写，不要 Write 整份 JSON；未知字段保持占位符 `VULNHUNTER_PENDING`。动态验证先跑当前 poc.py；靶场可用时 ConfirmVuln 会系统再跑一遍落盘脚本，退出码非 0 则拒绝确认，不要用 static_only 跳过。缺失/跑不通且需改写时才用 debug MCP 动态调试，不要一上来就挂 MCP。确认报告含 `## 互联网资产证明`（旧报告 `## 应用搜索指纹` 视为等价）且分别给出 FOFA / X 情报社区资产测绘语句（禁止「或」关系）；「基础环境搭建」应引用 `docs/lab.md`。应用指纹是项目级的（docs/app-fingerprints.json），不要每条洞重新识别。有漏洞环境且项目指纹仍缺标题/hash 时才 CollectLabFingerprints 升级共享指纹并写回本条（apply=true 或 ConfirmVuln 传 fofa_fingerprint/x_fingerprint），不要把「待运行环境确认」留到确认后，也不要为此打回。危害写过头（如 SSRF 未证明回显却写可读元数据）则本轮按观察面改报告再 Confirm。先 SearchOldVuln kind=found：同根因同危害的 pending 兄弟用 MergeIntoVuln(absorb=[...]) 再 Confirm；当前条是重复则 MergeIntoVuln(into=主报告id) 结束会话，不要打回/误报/Write 已确认报告。若 ConfirmVuln 提示疑似重复，优先 MergeIntoVuln；确认危害/鉴权不同仍要单独确认时再次 Confirm 并传 confirm_not_duplicate=true（仅提醒过后才接受）。仅当入口/sink/根因分析错了、需要重新读源码时才 ReturnToWorker（写清缺哪一块）；PoC 跑不通不要打回。完成后 ConfirmVuln（须标 attack_surface=frontend|backend；后台再标 required_account=user|admin；并按审核证据填写 impact、exploit_complexity、defense_status 用于最终严重度校准；还必须填写 submission_tier、submission_reason、root_cause_key；核对 config_premise，标错则传入 default|specific 纠正；duplicate_grouped 仅留给危害/鉴权不同的相关变体且须原样复用已有键）或 MarkFalsePositive。
记住双层判断：先确认默认部署下攻击者能否单独打出有害冲击（默认可利用，不是只碰到 sink），再判断有没有 CVE 价值。核对配置前提：官方已警示的风险配置不算 specific，仅在此类开关下才成立则误报。不可利用的路径逃逸/需种文件才成立的问题、项目配置/.env/compose 里的默认密码/弱口令，以及前端传输混淆 AES/公开下发密钥，直接误报；有服务端机密危害的源码硬编码密钥不要当默认密码误报。赏金模式下普通 CSRF（仅缺 token、低危状态变更）误报，仅 1-click（打开恶意页面即触发 RCE 或其他高危操作）可确认。低危害难利用项标 `low_impact`，不要一律标 `cve_candidate`。不要按漏洞类型映射严重度。SSRF 必须核对观察面：有回显才能写可读元数据/内网正文；仅状态码/时延/报错差别只算内网端口探测，`impact` 用 `limited_info`，不要按凭据窃取 Confirm。
