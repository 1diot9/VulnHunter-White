挖掘模式：${audit_mode_label}。${audit_mode_hint}
审计对象：${target_kind_label}。${target_kind_hint}
审核漏洞 ID=${vuln_id}
元数据: ${payload}
${unconstrained_note}
${lab_note}
动态验证计划: ${debug_plan}
本轮只审核漏洞，不要再搭建 Docker 靶场（环境轮已结束，见 docs/lab.md）。**第一步先验证靶场健康**（Read docs/lab.md、探测 target_url）；若刚完成修复，须先调用 RecordLabRepair 写入 docs/lab-repairs.md（失效原因+解决方案），再开始漏洞验证。靶场故障时调用 RequestLabRebuild(reason=...)，不要自己修 Docker。
请 Read vulns/${vuln_id}/report.md、advisory.md、request.http、poc.py。默认本轮收口：ConfirmVuln 或 MarkFalsePositive。需要本机 CLI 时 SearchTools 查已索引工具（dir + path 入口），再用 Shell 按绝对路径执行。需要看无源码 class/jar 时用 ListBytecode / DecompileJava（禁止 Shell 直调 jadx）。PoC 与报告包装由你改，不要打回 Worker：写死地址/命令则 Write 成 CLI（`python poc.py -u <target_url>`；必须有 `--proxy`（空则直连）；RCE 加 `-c/--cmd` 并打印回显；输出默认英语、须 `--zh` 切中文）后 Confirm 传 poc_code；纯库洞不要把 harness 内联/mock 抄进 poc.py，无 HTTP/安装面时不要补假 `-u/--proxy`；同链 payload 细节（编码、参数名、鉴权头）也本轮改。英文 GitHub Advisory 填表稿（`advisory.md`，对齐 `templates/vuln-advisory.md`）由你收口：缺段或不能直接粘进 GitHub Description 则 Write 后 Confirm 传 `advisory_md`；须含 `### Vulnerable code`（完整相对路径 + 源码原文）；`## Severity / CWE` 须含 CVSS 3.1 向量（只填度量；基础分由 ConfirmVuln 计算，不要手填分数）。CVE JSON（`cve.json`，对齐 `templates/cve.json`）用 `ReadCveRecord` / `SetCveRecordField` 逐字段填写，不要 Write 整份 JSON；未知字段保持占位符 `VULNHUNTER_PENDING`。描述须为英文详述（产品/版本、根因、入口→sink 链路、漏洞代码完整路径与源码原文、完整 HTTP 请求包或无 HTTP 面时的 API 调用；长串用占位符），supportingMedia 用 HTML（漏洞代码与 PoC 放 `<pre>`），不要一句话摘要。${verify_gate}确认报告含 `## 互联网资产证明`（旧报告 `## 应用搜索指纹` 视为等价）且分别给出 FOFA / X 情报社区资产测绘语句（禁止「或」关系）；「基础环境搭建」应引用 `docs/lab.md`。应用指纹是项目级的（docs/app-fingerprints.json），不要每条洞重新识别。有漏洞环境且项目指纹仍缺标题/hash 时才 CollectLabFingerprints 升级共享指纹并写回本条（apply=true 或 ConfirmVuln 传 fofa_fingerprint/x_fingerprint），不要把「待运行环境确认」留到确认后，也不要为此打回。危害写过头（如 SSRF 未证明回显或外带却写可读元数据）则本轮按观察面改报告再 Confirm。先 SearchOldVuln kind=found：同根因同危害的 pending 兄弟用 MergeIntoVuln(absorb=[...]) 再 Confirm；当前条是重复则 MergeIntoVuln(into=主报告id) 结束会话，不要打回/误报/Write 已确认报告。若 ConfirmVuln 提示疑似重复，优先 MergeIntoVuln；确认危害/鉴权不同仍要单独确认时再次 Confirm 并传 confirm_not_duplicate=true（仅提醒过后才接受）。仅当入口/sink/根因分析错了、需要重新读源码时才 ReturnToWorker（写清缺哪一块）；PoC 跑不通不要打回。完成后 ConfirmVuln（须标 attack_surface=frontend|backend；后台再标 required_account=user|admin；并填写 cvss_vector=CVSS:3.1/... 基础向量，不要手填分数；PR 必须与攻击面一致：前台 PR:N、后台 user PR:L、admin PR:H；XSS 默认 UI:R/S:C/C:L/I:L/A:N，不要因 Cookie/接管账户把 C/I 标 H；还必须填写 submission_tier、submission_reason（中文）、root_cause_key；核对 config_premise，标错则传入 default|specific 纠正；duplicate_grouped 仅留给危害/鉴权不同的相关变体且须原样复用已有键）或 MarkFalsePositive。
Worker 声称前台（auth_premise / 触发条件 / 标题含前台、无需登录、未授权、无认证）时，必须对照 docs/auth.md 与全局过滤器/拦截器/Security/Shiro/权限注解独立核验无认证可达：未带 Cookie/Token 能否到达 sink。方法无注解不够。核完其实要登录 → 不要标 attack_surface=frontend；默认口令按误报，其余成立则改标后台并改报告。不要为结束无约束路径把后台洞标成前台。
记住双层判断：先确认默认部署下攻击者能否单独打出有害冲击（默认可利用，不是只碰到 sink），再判断有没有 CVE 价值。核对配置前提：官方已警示的风险配置不算 specific，仅在此类开关下才成立则误报。不可利用的路径逃逸/需种文件才成立的问题、项目配置/.env/compose 里的默认密码/弱口令，以及前端传输混淆 AES/公开下发密钥，直接误报；有服务端机密危害的源码硬编码密钥不要当默认密码误报。赏金模式下普通 CSRF（仅缺 token、低危状态变更）误报，仅 1-click（打开恶意页面即触发 RCE 或其他高危操作）可确认。低危害难利用项标 `low_impact`，不要一律标 `cve_candidate`。不要按漏洞类型映射严重度。SSRF 必须核对观察面：有回显或外带内网信息才能写可读元数据/内网正文（二者危害同级）；仅状态码/时延/报错差别、或仅出网回调不含内网内容，只算内网端口探测，CVSS 的 C/I/A 不要按凭据窃取标 H。XSS 不要标成 9.x（C:H/I:H）。
