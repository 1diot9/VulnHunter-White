项目 ID=${project_id}。上一轮环境搭建未完成（stop_reason=${stop_reason}；error=${error}）。
继续搭建或复用 Docker 靶场，写出 env/env.json 后 FinishLab。被测应用用 src/ 当前最新代码，不要换成旧版应用镜像或旧 tag。
无法搭建则 FinishLab(skipped=true, reason=...)。不要审核漏洞。
