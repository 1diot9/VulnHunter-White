上一轮未完成，请继续历史漏洞爬虫落盘。只根据 workspace/ghsa_new.json 写文档，不要调用 WebSearch / SearchGHSA / SearchGitHubIssues。不要读源码，不要攒着，也不要扫框架 CVE 清单；落盘不会结束本会话。本轮完成再 WriteOldVuln(done=true, note=跳过说明)。（${stop_reason}/${error}）
